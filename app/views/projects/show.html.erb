<br>
<hr>
<h1><%= @project.title %></h1>

    <hr>
    <div class="card">
    <div class="card-body">
        <%= @project.position%>
    </div>
    </div>
 <%= button_to t("global.Destroy"), project_path(@project), method: :delete, class: 'btn btn-primary', data: { confirm: 'Are you sure?' }%>
<%= link_to t("global.Edit"), edit_project_path(@project), class: 'btn btn-primary'%> 
<%= link_to t("global.Back"), projects_path , class: 'btn btn-primary'%>
<br>
<%= link_to t("global.New_task"), :controller => :tasks, :action => :new, :id => @project.id%>